Booking API route missing. Need to create:
1. /app/api/book/route.js — Google Calendar + Brevo email integration
2. Add GoogleCalendarBooking to page.js
3. Fix Internal Server Error — env vars missing (MONGODB_URI, EMAIL_HOST, etc.)